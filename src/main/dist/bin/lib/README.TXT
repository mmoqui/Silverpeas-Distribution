In this directory put the local dependencies required by the installation of
Silverpeas and that they aren't provided by any Maven repository.
For example, a such dependency can be the Oracle JDBC driver library that
cannot be available in a Maven repository for license reason.
